name: MailOrganizer Api Build

on:
  push:
    branches: [ "main" ]
 
jobs:
  build-worker:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo code
      uses: actions/checkout@v2
    - name: npm install 
      run: npm install
    - run: npm run build