name: MailOrganizer Api Build

on:
  push:
    branches: [ "main" ]
 
jobs:
  build-worker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '18'
    - name: npm install 
      run: npm install
    - run: npm run build